neon-animation is required by iron-dropdown (a dependency of px-dropdown), but is not used in any way.

This bower_override eliminates the overhead of neon-animation by stubbing out the required methods.